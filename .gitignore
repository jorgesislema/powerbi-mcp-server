venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# ── Secrets / credenciales (NUNCA subir) ──
.env
.env.*
!.env.example
*.secret
secrets.json
credentials.json

# ── VS Code (solo excluir el json con rutas absolutas) ──
.vscode/mcp.json

# ── Archivos temporales Windows ──
*.pbix
Thumbs.db
desktop.ini
.ven

# ── Entornos virtuales ──
.venv/
venv/
env/

# ── Vendor DLLs (Windows-only, no versionar) ──
vendor/

# ── Build / distribución ──
dist/
build/
*.egg-info/
*.egg

# ── Tests y cobertura ──
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# ── Mypy / linters ──
.mypy_cache/
.ruff_cache/

# ── Artifacts generados por scripts ──
artifacts/
